package vn.edu.iuh.fit.backend.repositories;import org.springframework.data.jpa.repository.JpaRepository;import org.springframework.data.jpa.repository.Query;import org.springframework.stereotype.Repository;import vn.edu.iuh.fit.backend.entities.User;@Repositorypublic interface UserRepository  extends JpaRepository<User,Long> {    //Kiểm tra xem và mật khẩu có tồn tại không    @Query("SELECT CASE WHEN COUNT(u) > 0 THEN true ELSE false END FROM User u WHERE u.email = ?1 AND u.passwordHash = ?2")    boolean existsUserByEmailAndPassword(String email, String password);    //Lấy tên User dựa vào email    @Query("SELECT u.lastName FROM User u WHERE u.email = ?1")    String findNameByEmail(String email);    //Lấy thông tin người dùng từ email    User findByEmail(String email);}